package com.pwc.us.wsclient.config;

/**
 * This class solves the problem where every web service we set up clients against
 * need to have identical security and locale headers, but auto-generation of this code
 * means that this is not a safe method of storing password.
 * 
 */
public class DefaultHeaderValues {
    
    public static final String LOCALE = "en_US";
  // su portalapi
 // craxUp4E
    
}
